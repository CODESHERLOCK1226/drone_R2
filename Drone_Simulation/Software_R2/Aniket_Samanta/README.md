# WebSocket Drone Controller

This implementation uses WebSockets for real-time communication with the drone simulation.

## Features

- Real-time bidirectional communication via WebSockets
- PID controller for stable flight
- Waypoint navigation
- Emergency procedures
- JSON message format

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt